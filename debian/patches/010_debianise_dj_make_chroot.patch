Author: 
Description: Update script to replace choices with Debian values
diff -ur domjudge-3.3.0RC1.orig/etc/judgehost-config.php domjudge-3.3.0RC1/etc/judgehost-config.php
--- domjudge-3.3.0RC1.orig/etc/judgehost-config.php	2012-04-30 13:37:40.000000000 +0200
+++ domjudge-3.3.0RC1/etc/judgehost-config.php	2012-05-20 23:17:41.126405876 +0200
@@ -11,4 +11,4 @@
 // leave empty to disable. This example script can be used to support
 // Oracle (Sun) Java with a chroot (edit the script first!).
 // define('CHROOT_SCRIPT', 'chroot-startstop.sh');
-define('CHROOT_SCRIPT', '');
+define('CHROOT_SCRIPT', 'chroot-startstop.sh');
diff -ur domjudge-3.3.0RC1.orig/judge/chroot-startstop.sh domjudge-3.3.0RC1/judge/chroot-startstop.sh
--- domjudge-3.3.0RC1.orig/judge/chroot-startstop.sh	2012-04-30 13:37:40.000000000 +0200
+++ domjudge-3.3.0RC1/judge/chroot-startstop.sh	2012-05-20 23:17:22.229785896 +0200
@@ -18,7 +18,7 @@
 SUBDIRMOUNTS="etc lib usr"
 
 # Location where to bind mount from:
-CHROOTORIGINAL="/chroot/domjudge"
+CHROOTORIGINAL="/var/lib/domjudge/javachroot"
 
 case "$1" in
 	start)
Alleen in domjudge-3.3.0RC1/judge: chroot-startstop.sh.orig
diff -ur domjudge-3.3.0RC1.orig/misc-tools/dj_make_chroot domjudge-3.3.0RC1/misc-tools/dj_make_chroot
--- domjudge-3.3.0RC1.orig/misc-tools/dj_make_chroot	2012-04-30 13:37:40.000000000 +0200
+++ domjudge-3.3.0RC1/misc-tools/dj_make_chroot	2012-05-20 23:17:22.229785896 +0200
@@ -15,8 +15,8 @@
 set -e
 
 # Read command-line parameters:
-CHROOTDIR=$1
-ARCH=$2
+CHROOTDIR=/var/lib/domjudge/javachroot.new
+ARCH=`dpkg --print-architecture`
 
 # List of possible architectures to install chroot for:
 ARCHLIST="amd64,armel,i386,ia64,mips,mipsel,powerpc,s390,sparc"
@@ -66,6 +66,7 @@
 [ -z "$ARCH" ]      && error "No architecture given."
 [ -e "$CHROOTDIR" ] && error "'$CHROOTDIR' already exists, remove manually."
 
+rm -rf "$CHROOTDIR"
 mkdir -p "$CHROOTDIR"
 cd "$CHROOTDIR"
 CHROOTDIR="$PWD"
@@ -175,4 +176,6 @@
 
 umount "$CHROOTDIR/proc"
 
+mv /var/lib/domjudge/javachroot.new /var/lib/domjudge/javachroot
+
 exit 0
Alleen in domjudge-3.3.0RC1/misc-tools: dj_make_chroot.orig
